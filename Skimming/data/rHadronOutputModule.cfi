include "SUSYBSMAnalysis/RHadron/data/rHadron_EventContent.cff"

module rHadronOutputModule = PoolOutputModule {
  untracked string fileName = "rHadron.root"
  using rHadronEventContent
  using rHadronEventSelection
  untracked PSet dataset = {
    untracked string dataTier = "RECO"
    untracked string filterName = "rHadron"
  }
}
